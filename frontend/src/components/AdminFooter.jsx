import MarketingFooter from './MarketingFooter'

/** Full site footer inside admin layout (shrink-0 keeps flex column stable). */
const AdminFooter = () => <MarketingFooter footerClassName="shrink-0" />

export default AdminFooter
